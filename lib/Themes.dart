import 'package:flutter/material.dart';

ThemeData lightBlueTheme = ThemeData(
  primarySwatch: Colors.blue,
  hintColor: Colors.lightBlue,
  brightness: Brightness.light,
);

ThemeData darkPurpleTheme = ThemeData(
  primarySwatch: Colors.purple,
  hintColor: Colors.amber,
  brightness: Brightness.dark,
);

ThemeData greenBrownTheme = ThemeData(
  primaryColor: Colors.green,
  hintColor: Colors.brown,
  brightness: Brightness.light,
);

ThemeData redTealTheme = ThemeData(
  primaryColor: Colors.red,
  hintColor: Colors.teal,
  brightness: Brightness.light,
);

ThemeData customPinkTheme = ThemeData(
  primaryColor: Colors.pink,
  hintColor: Colors.deepPurple,
  brightness: Brightness.light,
  scaffoldBackgroundColor: Colors.grey[100],
  );